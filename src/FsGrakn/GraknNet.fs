namespace FsGrakn.CSharp

(*
    the plan is to have a OO wrapper for the functional implementation in FsGrakn.fs here
    this would be nicer for C# and VB.NET clients which normally aren't especially happy
    about a rahter functional F# implementation

    marked as TODO
*)
module Client =
    ()